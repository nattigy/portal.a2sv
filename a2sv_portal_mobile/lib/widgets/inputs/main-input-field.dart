import 'package:flutter/material.dart';

class MainInputField extends StatelessWidget {
  const MainInputField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TextField();
  }
}
