import 'package:flutter/material.dart';

class TopicsTab extends StatelessWidget {
  const TopicsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Topics"),),);
  }
}
