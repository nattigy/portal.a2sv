import 'package:flutter/material.dart';

class ContestTab extends StatelessWidget {
  const ContestTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Contest"),
      ),
    );
  }
}
