import 'package:flutter/material.dart';

class SingleStudent extends StatelessWidget {
  const SingleStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
